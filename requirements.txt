# ========== NEW LIFE GYM - Dependencias ==========

# Framework web
Flask==3.0.0

# Werkzeug (incluido con Flask)
Werkzeug==3.0.1

# Jinja2 para templates (incluido con Flask)
Jinja2==3.1.2

# MarkupSafe (dependencia de Jinja2)
MarkupSafe==2.1.3

# Click (para CLI de Flask)
click==8.1.7

# Itsdangerous (para sesiones seguras)
itsdangerous==2.1.2

# Blinker (para señales de Flask)
blinker==1.7.0

# Conexión a PostgreSQL (Neon)
psycopg2-binary==2.9.9